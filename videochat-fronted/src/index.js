import ReactDOM from "react-dom/client";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));

// Use the tag "App".
// And now we can use this function again and again in differents files in the project.
root.render(<App />);
